services:
  - type: web
    name: roast-code-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run main.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: 8501